# repo-market-model — standing rules

**See [`CLAUDE.md`](CLAUDE.md).** It is the single copy of the standing rules for any
coding agent working this repository, whichever tool loads it, and it begins by telling
you how to find out which checkout you are in.

This file is a pointer and holds no rules of its own. Two files of standing rules is one
file plus a way for them to disagree, which is the defect this repository keeps finding
one layer down: a registry that copies the parser's column names, a document that
restates a version `pyproject.toml` declares. Both are `HUMAN_ONLY`.
